@page
@model ChatModel
@{
    ViewData["Title"] = "Q & A";
}

<div class="container mt-4">
    <h2 class="mb-4">
        <i class="bi bi-chat-dots"></i> Document Q &amp; A
    </h2>

    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <form method="post">
                <div class="mb-3">
                    <label asp-for="Question" class="form-label fw-semibold">Your Question</label>
                    <textarea asp-for="Question"
                              class="form-control"
                              rows="3"
                              placeholder="Type your question about the document here..."
                              required></textarea>
                    <span asp-validation-for="Question" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="DocumentId" class="form-label fw-semibold">
                        Document ID <span class="text-muted fw-normal">(optional)</span>
                    </label>
                    <input asp-for="DocumentId"
                           type="number"
                           class="form-control"
                           placeholder="Enter an ID for a specific document, or leave blank to search all documents" />
                </div>

                <button type="submit" class="btn btn-primary px-4">
                    <span>Submit</span>
                </button>
            </form>
        </div>
    </div>

    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger" role="alert">
            <strong>Error:</strong> @Model.ErrorMessage
        </div>
    }

    @if (!string.IsNullOrEmpty(Model.Answer))
    {
        <div class="card border-success shadow-sm mb-4">
            <div class="card-header bg-success text-white fw-semibold">
                Answer
            </div>
            <div class="card-body">
                <p class="mb-0" style="white-space: pre-wrap;">@Model.Answer</p>
            </div>
        </div>

        @if (Model.Sources.Count > 0)
        {
            <div class="card shadow-sm">
                <div class="card-header fw-semibold">
                    Sources
                </div>
                <div class="card-body p-0">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Document Name</th>
                                <th>Page</th>
                                <th>Relevance</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var source in Model.Sources)
                            {
                                <tr>
                                    <td>@source.DocumentName</td>
                                    <td>@source.PageNumber</td>
                                    <td>
                                        <span class="badge bg-secondary">@source.Relevance.ToString("P1")</span>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        }
    }
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
